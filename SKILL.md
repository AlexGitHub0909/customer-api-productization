---
name: customer-api-productization
description: Assess whether an existing business capability is suitable for a customer or partner API, then build or harden a governed external API product with a stable contract, controlled access, safe environments, executable integration material, test evidence, operations support, and release gates. Use for suitability reviews, productization, onboarding, expansion, or pre-release hardening. Do not use for ordinary internal API implementation, one-off integrations, contract generation alone, or as the sole readiness process for a public developer platform.
---

# 客户 API 产品化交付

把已经存在、能够被证据验证的业务能力，整理并交付为客户可以长期接入、团队可以持续维护、运营可以安全开通的 API 产品。

这个 Skill 要交付一条可以核验的链路，覆盖产品边界、运行实现、外部契约、接入材料、测试证据和上线治理。单独生成几份文档不算完成。适用性或证据不足时，应停在评估或阻塞结论，不能用模板拼出一个虚假的“完整 API 产品”。

## 先判断是否适用

执行任何设计或写作前，读取 [applicability.md](references/applicability.md)，给出 `PRODUCTIZE`、`DISCOVERY_ONLY` 或 `NOT_SUITABLE` 结论。

进入完整产品化至少需要满足这些条件：

- 接入方是组织边界外的客户、合作伙伴或其受托系统，不是单纯的内部服务调用；
- 需求具有重复使用和持续维护价值，不是一次性数据交换或短期项目接口；
- 底层业务流程、状态和责任主体已经基本稳定，至少能支持受控试点；
- 客户身份、租户隔离、数据和动作授权边界可以定义并验证；
- 测试与生产影响可以隔离，或存在同等强度的受控联调方案；
- 产品、工程、运营和支持责任可以明确归属。

以下情况不要进入完整交付：

- 业务流程仍频繁推翻，连成功、失败和最终状态都无法稳定定义；
- 团队无权开放相关数据或动作，隐私、合规、商业和上游授权边界未确认；
- 无法识别客户主体，无法防止跨租户访问，或测试请求可能误写生产；
- 实际目标只是新增一个内部接口、生成一份 OpenAPI、包装上游原始接口或完成一次性对接；
- 没有可持续维护契约、处理故障和支持客户的责任人。

`DISCOVERY_ONLY` 可以输出事实盘点、风险、缺口和下一步验证计划；不得把它包装成可试点或可上线的交付包。

本 Skill 面向受控的 B2B 客户或合作伙伴 API。匿名注册、自助应用市场和开放生态型开发者平台可以复用其中的契约与安全门禁，但还需要独立补齐注册审核、滥用治理、开发者门户、法律条款、生态运营和大规模支持，不得仅凭本 Skill 判定平台完整。

## 选择工作模式

- `ASSESS`：只评估适配性、成熟度和阻塞项；
- `BASELINE`：从已有业务能力建立第一套客户 API 产品化交付；
- `EXPAND`：为已有客户 API 增加能力、事件、字段或新版本；
- `HARDEN`：在试点、正式开放或重要版本前做完整性与风险审查。

未指定时：已有客户 API 和契约用 `HARDEN`；已有业务实现但尚无可靠客户 API 基线用 `BASELINE`；明确新增能力用 `EXPAND`。

## 执行边界

- 用户只要求评估、审查或报告时，保持只读，输出结论、证据和缺口；
- 用户要求建设、补齐或修复时，在确认 `PRODUCTIZE` 后实施范围内所需的代码、配置、契约、测试、接入材料和运维能力；
- 发现硬阻塞时先收窄范围或转为 `EVIDENCE_BLOCKED`，不要用文档掩盖未实现能力；
- 部署、生产开通、数据迁移、客户通知和远程发布始终需要各自明确授权。

## 不预设项目结构

不要假设项目一定使用 REST、OpenAPI、单仓库、`src/`、`backend/` 或 `docs/`。先读取项目规则、仓库状态、构建与部署定义，再识别真实运行单元和事实来源。多仓项目应先确定共同工作区和本次可访问边界。

至少确认：

1. 外部入口和协议注册位置；
2. 认证、授权、租户和限流实现；
3. 业务编排、状态变化、异步任务和上游依赖；
4. 数据模型、敏感字段、审计和保留策略；
5. 契约、示例、测试、发布和运维材料；
6. 哪些能力已实现、仅计划、已废弃或无法访问。

读取 [evidence-workflow.md](references/evidence-workflow.md) 建立证据链。目录名、旧文档和接口命名只能作为线索，不能单独证明事实。

## 强制阶段门禁

按顺序执行。前一阶段未通过，不得把后一阶段写成完成。

### 1. `FIT`

明确接入对象、重复使用价值、能力成熟度、授权边界、环境能力和责任人，输出适配结论与理由。

### 2. `DISCOVERY`

恢复项目上下文，从外部入口追到业务编排、数据、异步流程、上游依赖、测试和现有文档。记录不可访问范围。

### 3. `PRODUCT_BOUNDARY`

定义目标客户、使用场景、开放能力、非目标、数据与动作边界、服务责任、成功指标和阶段目标；确认隐私、合规、数据保留、上游授权及适用商业边界。不要直接暴露内部页面接口或上游原始契约。

### 4. `CONTRACT`

建立适合当前协议的机器可读契约。同步定义认证、租户、字段、枚举、时间和金额、分页、错误、幂等、限流、版本兼容、异步结果和文件规则。

### 5. `ACCESS_AND_ENVIRONMENTS`

设计凭证签发、展示、轮换、暂停、吊销、scope、审计和应急停用。验证沙箱与生产的外部契约尽量同构，但数据和副作用严格分离。

### 6. `DEVELOPER_EXPERIENCE`

提供从拿到凭证到完成首个业务闭环的接入指南、可执行示例、错误处理、Webhook 或事件说明、变更记录和上线清单。示例必须消费真实返回值，不得依赖虚构编号。

### 7. `VERIFICATION`

建立契约校验、运行时一致性、鉴权矩阵、租户隔离、负向与边界、幂等、限流、沙箱隔离、事件投递和敏感信息测试。读取 [quality-gates.md](references/quality-gates.md)。

### 8. `OPERATIONS_AND_RELEASE`

补齐请求标识、脱敏日志、指标、告警、容量、配额、依赖降级、目标服务水平、支持入口、故障处理、开通审批、回滚、版本和废弃策略。API 交付包就绪不等于已经部署、开通生产或通知客户。

### 9. `RECONCILE`

按 [delivery-contract.md](references/delivery-contract.md) 核对产品边界、机器契约、运行实现、示例、测试、操作手册和发布记录。修复冲突后，再运行确定性校验器。

## 必须交付的核心内容

文件名称和目录沿用项目现有约定，不另建平行事实源。完整产品化通常必须覆盖：

- 产品范围与适配结论；
- 客户可见的机器契约；
- 认证、租户、环境和凭证生命周期；
- 接入指南、错误目录、可执行示例和变更记录；
- 安全模型、测试矩阵与当前验证报告；
- 运行监控、支持、故障和发布手册；
- 需求、契约、实现、测试和文档之间的追溯关系。

Webhook、文件下载、异步写入、批量任务和 SDK 只在产品确实包含相应能力时交付。标记 `not_applicable` 必须说明业务原因，不能用于绕过实现缺口。

SDK 不是默认交付物。只有目标客户、语言分布、维护责任、版本发布和安全响应能力都明确时，才建立正式 SDK；否则优先交付经过真实运行验证的轻量示例。

## 关键质量规则

- 契约描述当前客户实际可调用的行为，不复制内部模型或上游供应商原始结构；
- 客户错误稳定、可处理，不暴露堆栈、SQL、内部路径、供应商负载或安全细节；
- 可重试写操作有幂等边界；结果未知的外部写入不得盲目重放；
- 沙箱可以有状态，但不得写入生产业务、触发真实收费或调用真实副作用；
- Webhook 或事件必须有稳定事件标识、签名、重放防护、投递记录和明确的未知结果策略；
- 长期共享密钥、静态 API key 和恢复码只在必要时明文展示一次；其它凭证按各自生命周期处理，存储、日志和页面均不得泄露；
- 客户可见价格、状态和文件由平台契约表达，不泄露内部成本、路由策略或私有存储位置；
- 示例、集合和文档中的请求必须可执行，并与当前机器契约及运行时返回一致；
- “已测试”必须对应当前版本的新鲜执行结果，不能只引用测试文件存在；
- 正式开通、部署、迁移、通知客户和远程发布均需要各自明确授权。

涉及幂等、异步写入、Webhook、沙箱和受保护文件时，读取 [integration-safety.md](references/integration-safety.md)。

## 确定性交付检查

根据 [manifest-format.md](references/manifest-format.md) 创建临时 JSON 清单并运行：

```bash
python3 /path/to/customer-api-productization/scripts/validate_productization.py \
  /path/to/productization-manifest.json \
  --workspace /path/to/workspace
```

清单是验证输入，不必成为项目长期事实源。校验器只检查结构、证据引用、交付物存在和声明的门禁；它不能代替协议验证器、运行测试、安全审查或人工产品判断。

声明试点或 GA 交付包就绪时，清单必须绑定本次审查范围、源代码或发布版本、带时区的审查时间和本次接受的最早证据时间。契约校验、运行时一致性、示例执行、测试、安全、容量、试点和回滚类门禁必须引用相应类型的执行证据和实际观察时间，不能只引用说明文档。

## 完成状态

- `ASSESSMENT_COMPLETE`：用户只要求评估，适配结论、依据和下一步已明确；
- `PILOT_PACKAGE_READY`：受控客户试点需要的交付和证据已通过；
- `GA_PACKAGE_READY`：正式规模化开放所需的兼容、支持、试点、回滚和发布门禁已通过；
- `EVIDENCE_BLOCKED`：关键事实、授权或验证缺失，已列出影响和最短解阻路径。

`PILOT_PACKAGE_READY` 和 `GA_PACKAGE_READY` 只表示交付包达到相应门槛，不表示已经部署、批准或对客户开放。

## 最终交付说明

最终答复至少说明：

- 模式、适配结论和适用范围；
- 新增或更新的交付物；
- 关键产品与安全边界；
- 实际执行的验证及结果；
- 未验证、阻塞或明确不适用的内容；
- Git、远程文档、部署和生产开通状态；
- 最终状态。

不要只说“API 已产品化”。说明它已达到试点还是 GA 交付门槛、依据什么证据，以及哪些生产动作尚未发生。
